import "$lib/scripts/extensions/string"
import type { ISolution } from "../solution"


export class Day00 implements ISolution {
    solve1 = (input: string): string => {
        return ''
    }

    solve2 = (input: string): string => {
        return ''
    }
}